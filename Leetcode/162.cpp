#include <iostream>
using namespace std;

int main() {
    vector<int> arr = {1,2,3,1};
    // we need to find the peak element in te array
    int peak = -1;
    for(int i = 0 ; i < arr.size(); i++){
       
    }

    cout << peak;
    
    return 0;
}